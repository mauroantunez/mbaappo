<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.mbaappo.mbaappo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.MainActivity"
            android:label="@string/pagina_principa"
            android:theme="@style/AppTheme.NoActionBar" >

        </activity>
        <activity android:name=".UI.inicio" />
        <activity
            android:name=".UI.activity_registro_usuarios"
            android:label="@string/registrar" />
        <activity
            android:name=".UI.CrearEditarServicio"
            android:label="@string/publicar_servicio" />
        <activity
            android:name=".UI.Perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.c_contrasenha"
            android:label="@string/m_contrasena" />
        <activity
            android:name=".UI.Servicio"
            android:label="Servicio" />
        <activity
            android:name=".UI.editar_datos"
            android:label="@string/e_dato" />
        <activity
            android:name=".List.Lista_servicios"
            android:launchMode="singleTop"
            android:label="Servicios" />
        <activity
            android:name=".List.list_historial"
            android:label="Historial" />
        <activity
            android:name=".UI.servicios_realizados"
            android:label="Servicios Ofrecidos" />
        <activity
            android:name=".UI.inicio_principal"
            android:label="Bienvenido">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.mensajeria" />
        <activity android:name=".UI.Seleccionar_foto" />
        <activity
            android:name=".UI.servicio_solicitado"
            android:label="Mensajes" />
        <activity
            android:name=".UI.lista_mensaje_historial"
            android:label="Mensajes" />
        <activity android:name=".List.buscador" />
        <activity
            android:name=".UI.olvidar_contrasenha"
            android:label="Restablecer Contraseña" />
        <activity
            android:name=".UI.editar_servicios"
            android:label="Editar Servicio" />
        <activity android:name=".List.list_categoria_servicio" />

        <service android:name=".UI.NotificationService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".UI.NotificacionIDTokenService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>